<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.booktodolist.bestSeller.BestSellerMapper">
    <insert id="insertBestseller">
        INSERT INTO book
        SET icate = #{categoryName}
        , title = #{title}
        , writer = #{author}
        , publisher = #{publisher}
        , isbn = #{isbn13}
        , totalpage = #{totalpage}
    </insert>
</mapper>